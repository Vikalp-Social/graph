# Copy to .env and fill in. run.sh loads Topic-Cluster/.env first, then this file.

GEMINI_API_KEY=
# Default in run.sh / ml-service is Gemma on the Gemini API. Override if needed:
# GEMINI_MODEL=gemma-3-27b-it
# GEMINI_MODEL=gemma-2-9b-it
# GEMINI_MODEL=gemini-2.5-flash
# LLM_PROVIDER=gemini
